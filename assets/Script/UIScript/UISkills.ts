import { UIWindow } from "../UIFrame/UIForm";

const {ccclass, property} = cc._decorator;

@ccclass
export default class UISkills extends UIWindow {

    static prefabPath = "Forms/Windwos/UISkills";

    // onLoad () {}

    start () {

    }

    // update (dt) {}
}
