import NetManager from "./NetManager";
import { IMsg } from "./NetInterface";

// NetManager.getInstance().connect("ws://127.0.0.1:ws", 1);
/**
 * 
NetManager.getInstance().request(null, (msg: IMsg) => {
    
}, this);
 */